<h1>Add new task</h1>
<form class="new-task" {{on "submit" this.createTask}}>
  <AutofocusElasticTextarea
    @value={{this.newTaskDescription}}
    @insert-newline={{action this.createTask}}
    @escape-press={{@cancel}}
    data-test-new-task-description
    placeholder="New task"
  />
  <br><br>
  <div data-test-new-task-date>
    <PowerDatepicker @selected={{this.newTaskDate}} @onSelect={{action this.changeDate}} @renderInPlace={{true}} as |dp|>
      <dp.Trigger>
        <input type="text" placeholder="Date" value={{moment-format this.newTaskDate "YYYY-MM-DD" allow-empty=true}} readonly>
      </dp.Trigger>
      <dp.Content>
        <div class="date-picker-content">
          <dp.Nav />
          <dp.Days />
        </div>
      </dp.Content>
    </PowerDatepicker>
  </div>
  <br><br>
  <input type="submit" value="Add" data-test-create-task>
  <button type="button" {{action @cancel}} data-test-cancel-button>
    Cancel
  </button>
</form>
